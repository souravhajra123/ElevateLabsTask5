apiVersion: v1
kind: Service
metadata:
  name: np-svc
  labels:
    app: calci
spec:
  type: NodePort
  selector:
    app: calci
  ports:
    - protocol: TCP
      port: 80         # Service Port
      targetPort: 80   # Pod Port
      nodePort: 30080  # Exposes the service on Node's port 30080
  type: NodePort
